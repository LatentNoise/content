apiVersion: v2
name: content
description: Content — le moteur qui transforme des sources en artefacts (vidéo, audio, sous-titres, transcript, résumé, Markdown, PDF)
type: application
# version  : la version DU CHART (change quand les templates changent)
# appVersion : la version de Content déployée par défaut (le tag de l'image)
version: 0.1.0
appVersion: "0.7.1"
home: https://github.com/LatentNoise/content
sources:
  - https://github.com/LatentNoise/content
