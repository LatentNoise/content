apiVersion: v2
name: content
description: Content — the engine that turns sources into artifacts (video, audio, subtitles, transcript, summary, Markdown, PDF)
type: application
# version    : the version OF THE CHART (bump when the templates change)
# appVersion : the default version of Content deployed (the image tag)
version: 0.1.0
appVersion: "0.8.2"
home: https://github.com/LatentNoise/content
sources:
  - https://github.com/LatentNoise/content
