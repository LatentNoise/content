apiVersion: v2
name: mailer
description: LatentNoise Mailer — the single outbound-email door for every product.
type: application
# version    : the version OF THE CHART (bump when the templates change)
# appVersion : the default image tag, i.e. the version of the service deployed
version: 0.1.0
appVersion: "0.1.0"
home: https://github.com/LatentNoise/content
sources:
  - https://github.com/LatentNoise/content
